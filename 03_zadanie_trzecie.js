// Napisz funkcję która zwróci liczbę wszystkich unikalnych możłiwych dróg na siatce o dwóch rówńych długościach `grid_size`.
// Robot może się poruszać w każdym z czterech kierunków (w górę, w prawo, w dół, w lewo), oraz 
//   każde pole może odwiedzić tylko raz w czasie jednego przejścia.
// Robot zaczyna w lewym górnym rogu, a jego celem jest prawy dolny róg siatki.
//
// 2 ≤ `grid_size` ≤ 8

export default function(grid_size) {
   return 0;
}
